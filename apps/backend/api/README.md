# ⚠️ DEPRECATED — Python/FastAPI Backend

> **This backend has been superseded by the Spring Boot backend at [`apps/backend/spring-apis/`](../spring-apis/).**

The Java/Spring Boot 4 backend is the finalized production backend. This Python codebase is retained for historical reference only and is **no longer maintained**.

## Migration

All API functionality is now available in the Spring Boot backend:

```bash
# The active backend:
apps/backend/spring-apis/
```

See the [main README](../../README.md) for current documentation and quick start instructions.
