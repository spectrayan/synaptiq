type: object
description: Request body for AI-powered workflow generation
required: [prompt]
properties:
  prompt:
    type: string
    minLength: 10
    maxLength: 5000
    description: Natural language description of the desired workflow (10-5000 chars)
