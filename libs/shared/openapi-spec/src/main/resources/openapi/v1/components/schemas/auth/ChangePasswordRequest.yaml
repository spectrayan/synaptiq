type: object
required: [currentPassword, newPassword]
properties:
  currentPassword:
    type: string
    format: password
    minLength: 1
    maxLength: 128
  newPassword:
    type: string
    format: password
    minLength: 8
    maxLength: 128
