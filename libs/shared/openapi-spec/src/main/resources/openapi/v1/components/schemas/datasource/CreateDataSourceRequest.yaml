type: object
description: Create a new data source
required:
  - name
  - type
properties:
  name:
    type: string
    minLength: 1
    maxLength: 200
  description:
    type: string
    maxLength: 500
  type:
    $ref: '../common/enums/DataSourceType.yaml'
  connection:
    $ref: './ConnectionConfigResponse.yaml'
  credentialRef:
    $ref: './CredentialRefResponse.yaml'
