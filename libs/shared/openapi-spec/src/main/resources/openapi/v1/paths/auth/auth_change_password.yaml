post:
  operationId: changePassword
  summary: Change password
  tags: [Auth]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/auth/ChangePasswordRequest.yaml'
  responses:
    '204':
      description: Password changed
