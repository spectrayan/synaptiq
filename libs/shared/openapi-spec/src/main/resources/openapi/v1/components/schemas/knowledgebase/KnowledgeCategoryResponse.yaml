type: object
required:
  - id
  - name
properties:
  id:
    type: string
    description: Unique identifier for the category
  name:
    type: string
    description: Name of the category (e.g. HR, Engineering)
  description:
    type: string
    description: Optional description of the category
