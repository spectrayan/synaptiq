get:
  operationId: getTokenUsage
  summary: Get token usage
  tags: [Analytics]
  parameters:
    - $ref: '../../components/parameters/TenantIdHeader.yaml'
    - name: from
      in: query
      schema:
        type: string
        format: date
    - name: to
      in: query
      schema:
        type: string
        format: date
  responses:
    '200':
      description: Token usage
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/analytics/TokenUsageResponse.yaml'
