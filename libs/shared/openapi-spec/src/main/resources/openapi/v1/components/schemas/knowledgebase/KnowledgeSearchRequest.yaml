type: object
required:
  - query
properties:
  query:
    type: string
    description: The search query string
  categoryId:
    type: string
    description: Optional category to narrow down search
  tags:
    type: array
    items:
      type: string
    description: Optional tags to filter by
  topK:
    type: integer
    default: 5
