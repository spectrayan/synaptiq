[project]
name = "python-sdk"
version = "0.1.0"
description = "Synaptiq shared Python types and utilities"
requires-python = ">=3.12,<4"

[tool.hatch.build.targets.wheel]
packages = ["src/synaptiq_sdk"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
